repos:
- id: /.*/
  apply_requirements: [approved, mergeable]
  allowed_overrides: [apply_requirements, workflow]
  allow_custom_workflows: true

workflows:
  dev:
    plan:
      steps:
      - run: 'echo "helloworld.txt" '
      - run: my custom apply command
      - init
      - plan
